name: public-company-rag
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip>=23.0
  - pip:
    # Vector database
    - turbopuffer

    # OpenAI for embeddings and LLM
    - openai

    # Configuration management
    - pydantic>=2.0
    - pydantic-settings
    - python-dotenv

    # Data handling
    - datasets>=2.16.0  # Hugging Face datasets
    - pandas>=2.0.0
    - pyarrow>=14.0.0   # Parquet support

    # CLI and progress
    - tqdm
